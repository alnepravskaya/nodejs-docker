{
  "name": "task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "docker-compose -f docker-compose.yml up --attach node --force-recreate",
    "start:single": "docker-compose -f docker-compose-single-service.yml up --attach todo --force-recreate",
    "start:single-vue": "docker-compose -f docker-compose-vue-single-service.yml up --attach todo --force-recreate"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "uuid": "^9.0.0"
  }
}
